import { JoinCenter } from "@/components/center/JoinCenter";

/** Adhésion à un centre par saisie manuelle de la clé d'invitation. */
export default function JoinPage() {
  return (
    <div className="mx-auto max-w-md px-4 py-12">
      <JoinCenter />
    </div>
  );
}
